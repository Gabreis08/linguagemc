/*Primeiro arquivo em linguagem c.
Para iniciar primeiro devemos importar a biblioteca stdio (std-standard(padrão) io-input output
(entrada e saida). esta biblioteca possui os comandos para a interação com o teclado e monitor*/
#include <stdio.h>
/* iniciar a estrutura  do codigo para a  mensagem hello world, usando a função main(pricipal),pois o compilador c ira procurar
esta função para dar execução no nosso codigo.*/
int main(){
printf("Hello world!\n");
return 0;
}